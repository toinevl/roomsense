{
  "name": "roomsense",
  "private": true,
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "seed:local": "pnpm --filter @roomsense/seed seed:local",
    "seed:azure": "pnpm --filter @roomsense/seed seed:azure"
  }
}
